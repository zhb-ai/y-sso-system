# 此文件夹下放一些在domain中无法处理的业务逻辑，例如：
# - 认证应用服务（AuthApplicationService）
# - 用户应用服务（UserApplicationService）
# 以及跨domain的服务，例如：
# - 跨domain的认证服务（CrossDomainAuthService）
# - 跨domain的用户服务（CrossDomainUserService）